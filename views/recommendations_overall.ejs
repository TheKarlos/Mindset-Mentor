<html>

<head>
    <% include ../partials/head %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/chart.js@2.8.0/dist/Chart.min.css">
    <style>
        .rec-image-small {
            height: 75px;
            border: 0;
        }

        .rec-image-small:hover,
        .rec-image-small:focus {
            height: 100px;
            z-index: 1;
            margin-bottom: -25px;
        }

        .rec-image-active {
            height: 100px;
            z-index: 1;
            margin-bottom: -25px;
        }

        .rec-card-border {
            border: 4px solid #92afd7;
            border-radius: 8px;
        }
    </style>
</head>

<body>
    <div class="d-flex flex-column align-items-center justify-content-center h-100">
        <h1 class="px-5 py-2 text-white text-center border-bottom my-3">Recommendations and goals</h1>
        <div class="w-100 d-flex flex-row justify-content-around">
            <img class="rec-image-small" src="/sociability.png">
            <img class="rec-image-small" src="/productivity.png">
            <img class="rec-image-small rec-image-active" src="/activeness.png">
            <img class="rec-image-small" src="/happiness.png">
            <img class="rec-image-small" src="/sociability.png">
            <img class="rec-image-small" src="/sociability.png">
        </div>
        <div class="container card px-2 py-4 my-2 d-flex flex-row bg-secondary">
            <div class="d-flex flex-column w-50">
                <h3 class="text-white">Activeness</h3>
                <div class="card px-1 py-2 my-2 md-flex flex-row bg-primary rounded">
                    <h3>Try going for a jog this week</h3>
                </div>
                <div class="card px-1 py-2 my-2 md-flex flex-row bg-primary rounded">
                    <h3>Try going for a jog this week</h3>
                </div>
                <div class="card px-1 py-2 my-2 md-flex flex-row bg-primary rounded">
                    <h3>Try going for a jog this week</h3>
                </div>
            </div>
            <div class="d-flex flex-column w-50">
                <canvas id="myChart"></canvas>
            </div>
        </div>
        <% include ../partials/scripts %>
        <script src="https://cdn.jsdelivr.net/npm/chart.js@2.8.0/dist/Chart.min.js"></script>
        <script>
            var data = {
                labels: [1, 2, 3, 4],
                datasets: [{
                    data: [2, 2, 3, 5],
                    label: "Activity",
                    borderColor: "#3e95cd",
                    fill: false
                }]
            }
            var options = {
                title: {
                    display: true,
                    text: 'Activeness'
                },
                scales: {
                    yAxes: [{
                        ticks: {
                            stepSize: 1
                        }
                    }]
                }
            }
            var ctx = document.getElementById('myChart');
            var myLineChart = new Chart(ctx, {
                type: 'line',
                data: data,
                options: options
            });
        </script>
</body>

</html>